# explore-app

Systematically crawl a running iOS app to map screens, discover blind spots, and recommend adapter config.

Use `look` to observe the current screen, then `tap` or `navigate` to move through the app, recording each screen you visit.
